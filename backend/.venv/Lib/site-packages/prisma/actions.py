# -*- coding: utf-8 -*-
# code generated by Prisma. DO NOT EDIT.
# pyright: reportUnusedImport=false
# fmt: off
from __future__ import annotations

# global imports for type checking
from builtins import bool as _bool
from builtins import int as _int
from builtins import float as _float
from builtins import str as _str
import sys
import decimal
import datetime
from typing import (
    TYPE_CHECKING,
    Optional,
    Iterable,
    Iterator,
    Sequence,
    Callable,
    ClassVar,
    NoReturn,
    TypeVar,
    Generic,
    Mapping,
    Tuple,
    Union,
    List,
    Dict,
    Type,
    Any,
    Set,
    overload,
    cast,
)
from typing_extensions import TypedDict, Literal


LiteralString = str
# -- template actions.py.jinja --
from typing import TypeVar
import warnings

from . import types, errors, bases
from ._compat import model_parse
from ._constants import CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED

if TYPE_CHECKING:
    from .client import Prisma
    from .bases import _PrismaModel


_PrismaModelT = TypeVar('_PrismaModelT', bound='_PrismaModel')


class FavoriteActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    async def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.Favorite]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = await Favorite.prisma().query_raw(
            'SELECT * FROM Favorite WHERE id = $1',
            'bbadfchfja',
        )
        ```
        """
        return await self._client.query_raw(query, *args, model=self._model)

    async def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.Favorite
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = await Favorite.prisma().query_first(
            'SELECT * FROM Favorite WHERE userId = $1',
            'dhheabfhf',
        )
        ```
        """
        return await self._client.query_first(query, *args, model=self._model)

    async def create(
        self,
        data: types.FavoriteCreateInput,
        include: Optional[types.FavoriteInclude] = None
    ) -> _PrismaModelT:
        """Create a new Favorite record.

        Parameters
        ----------
        data
            Favorite record data
        include
            Specifies which relations should be loaded on the returned Favorite model

        Returns
        -------
        prisma.models.Favorite
            The created Favorite record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a Favorite record from just the required fields
        favorite = await Favorite.prisma().create(
            data={
                # data to create a Favorite record
                'userId': 'ggciceaie',
                'title': 'bbehjachib',
                'url': 'cadfabfehe',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def create_many(
        self,
        data: List[types.FavoriteCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple Favorite records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of Favorite record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = await Favorite.prisma().create_many(
            data=[
                {
                    # data to create a Favorite record
                    'userId': 'dgiiaaijj',
                    'title': 'bfaiacjjfc',
                    'url': 'eigcfgbif',
                },
                {
                    # data to create a Favorite record
                    'userId': 'bagcfbhiig',
                    'title': 'cghideieh',
                    'url': 'biabhbdai',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = await self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    async def delete(
        self,
        where: types.FavoriteWhereUniqueInput,
        include: Optional[types.FavoriteInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single Favorite record.

        Parameters
        ----------
        where
            Favorite filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned Favorite model

        Returns
        -------
        prisma.models.Favorite
            The deleted Favorite record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        favorite = await Favorite.prisma().delete(
            where={
                'id': 'idghgaicb',
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def find_unique(
        self,
        where: types.FavoriteWhereUniqueInput,
        include: Optional[types.FavoriteInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique Favorite record.

        Parameters
        ----------
        where
            Favorite filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Favorite model

        Returns
        -------
        prisma.models.Favorite
            The found Favorite record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        favorite = await Favorite.prisma().find_unique(
            where={
                'id': 'fjfddhigg',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    async def find_unique_or_raise(
        self,
        where: types.FavoriteWhereUniqueInput,
        include: Optional[types.FavoriteInclude] = None
    ) -> _PrismaModelT:
        """Find a unique Favorite record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            Favorite filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Favorite model

        Returns
        -------
        prisma.models.Favorite
            The found Favorite record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        favorite = await Favorite.prisma().find_unique_or_raise(
            where={
                'id': 'hjaecfifb',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.FavoriteWhereInput] = None,
        cursor: Optional[types.FavoriteWhereUniqueInput] = None,
        include: Optional[types.FavoriteInclude] = None,
        order: Optional[Union[types.FavoriteOrderByInput, List[types.FavoriteOrderByInput]]] = None,
        distinct: Optional[List[types.FavoriteScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple Favorite records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of Favorite records returned
        skip
            Ignore the first N results
        where
            Favorite filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Favorite model
        order
            Order the returned Favorite records by any field
        distinct
            Filter Favorite records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.Favorite]
            The list of all Favorite records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 Favorite records
        favorites = await Favorite.prisma().find_many(take=10)

        # find the first 5 Favorite records ordered by the title field
        favorites = await Favorite.prisma().find_many(
            take=5,
            order={
                'title': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    async def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.FavoriteWhereInput] = None,
        cursor: Optional[types.FavoriteWhereUniqueInput] = None,
        include: Optional[types.FavoriteInclude] = None,
        order: Optional[Union[types.FavoriteOrderByInput, List[types.FavoriteOrderByInput]]] = None,
        distinct: Optional[List[types.FavoriteScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single Favorite record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Favorite filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Favorite model
        order
            Order the returned Favorite records by any field
        distinct
            Filter Favorite records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Favorite
            The first Favorite record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Favorite record ordered by the url field
        favorite = await Favorite.prisma().find_first(
            skip=1,
            order={
                'url': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    async def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.FavoriteWhereInput] = None,
        cursor: Optional[types.FavoriteWhereUniqueInput] = None,
        include: Optional[types.FavoriteInclude] = None,
        order: Optional[Union[types.FavoriteOrderByInput, List[types.FavoriteOrderByInput]]] = None,
        distinct: Optional[List[types.FavoriteScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single Favorite record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Favorite filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Favorite model
        order
            Order the returned Favorite records by any field
        distinct
            Filter Favorite records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Favorite
            The first Favorite record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Favorite record ordered by the memo field
        favorite = await Favorite.prisma().find_first_or_raise(
            skip=1,
            order={
                'memo': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update(
        self,
        data: types.FavoriteUpdateInput,
        where: types.FavoriteWhereUniqueInput,
        include: Optional[types.FavoriteInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single Favorite record.

        Parameters
        ----------
        data
            Favorite record data specifying what to update
        where
            Favorite filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned Favorite model

        Returns
        -------
        prisma.models.Favorite
            The updated Favorite record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        favorite = await Favorite.prisma().update(
            where={
                'id': 'cbbbjbfcii',
            },
            data={
                # data to update the Favorite record to
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def upsert(
        self,
        where: types.FavoriteWhereUniqueInput,
        data: types.FavoriteUpsertInput,
        include: Optional[types.FavoriteInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            Favorite filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned Favorite model

        Returns
        -------
        prisma.models.Favorite
            The created or updated Favorite record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        favorite = await Favorite.prisma().upsert(
            where={
                'id': 'bbejhfidcb',
            },
            data={
                'create': {
                    'id': 'bbejhfidcb',
                    'userId': 'bagcfbhiig',
                    'title': 'cghideieh',
                    'url': 'biabhbdai',
                },
                'update': {
                    'userId': 'bagcfbhiig',
                    'title': 'cghideieh',
                    'url': 'biabhbdai',
                },
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update_many(
        self,
        data: types.FavoriteUpdateManyMutationInput,
        where: types.FavoriteWhereInput,
    ) -> int:
        """Update multiple Favorite records

        Parameters
        ----------
        data
            Favorite data to update the selected Favorite records to
        where
            Filter to select the Favorite records to update

        Returns
        -------
        int
            The total number of Favorite records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all Favorite records
        total = await Favorite.prisma().update_many(
            data={
                'createdAt': datetime.datetime.utcnow()
            },
            where={}
        )
        ```
        """
        resp = await self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    async def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.FavoriteWhereInput] = None,
        cursor: Optional[types.FavoriteWhereUniqueInput] = None,
    ) -> int:
        """Count the number of Favorite records present in the database

        Parameters
        ----------
        select
            Select the Favorite fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Favorite filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.FavoriteCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await Favorite.prisma().count()

        # results: prisma.types.FavoriteCountAggregateOutput
        results = await Favorite.prisma().count(
            select={
                '_all': True,
                'updatedAt': True,
            },
        )
        ```
        """


    @overload
    async def count(
        self,
        select: types.FavoriteCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.FavoriteWhereInput] = None,
        cursor: Optional[types.FavoriteWhereUniqueInput] = None,
    ) -> types.FavoriteCountAggregateOutput:
        ...

    async def count(
        self,
        select: Optional[types.FavoriteCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.FavoriteWhereInput] = None,
        cursor: Optional[types.FavoriteWhereUniqueInput] = None,
    ) -> Union[int, types.FavoriteCountAggregateOutput]:
        """Count the number of Favorite records present in the database

        Parameters
        ----------
        select
            Select the Favorite fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Favorite filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.FavoriteCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await Favorite.prisma().count()

        # results: prisma.types.FavoriteCountAggregateOutput
        results = await Favorite.prisma().count(
            select={
                '_all': True,
                'id': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = await self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.FavoriteCountAggregateOutput, resp['data']['result']['_count'])

    async def delete_many(
        self,
        where: Optional[types.FavoriteWhereInput] = None
    ) -> int:
        """Delete multiple Favorite records.

        Parameters
        ----------
        where
            Optional Favorite filter to find the records to be deleted

        Returns
        -------
        int
            The total number of Favorite records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all Favorite records
        total = await Favorite.prisma().delete_many()
        ```
        """
        resp = await self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    async def group_by(
        self,
        by: List['types.FavoriteScalarFieldKeys'],
        *,
        where: Optional['types.FavoriteWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.FavoriteAvgAggregateInput'] = None,
        sum: Optional['types.FavoriteSumAggregateInput'] = None,
        min: Optional['types.FavoriteMinAggregateInput'] = None,
        max: Optional['types.FavoriteMaxAggregateInput'] = None,
        having: Optional['types.FavoriteScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.FavoriteCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.FavoriteScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.FavoriteScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.FavoriteGroupByOutput']:
        """Group Favorite records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar Favorite fields to group records by
        where
            Favorite filter to select records
        take
            Limit the maximum number of Favorite records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.FavoriteGroupByOutput]
            A list of dictionaries representing the Favorite record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group Favorite records by userId values
        # and count how many records are in each group
        results = await Favorite.prisma().group_by(
            ['userId'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = await self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]



def _select_fields(root: str, select: Mapping[str, Any]) -> str:
    """Helper to build a GraphQL selection string

    This is a work around until field selection is added to the query builder.
    """

    return root + ' {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))


from . import models